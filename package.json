import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import ProjectSetupStatus from '../src/components/ProjectSetupStatus'; // Component does not exist yet, causing initial failure

describe('ProjectSetupStatus', () => {
  it('should display confirmation of Vite, React, TypeScript, and ESLint setup', () => {
    render(<ProjectSetupStatus />);

    expect(screen.getByText(/Vite project initialized/i)).toBeInTheDocument();
    expect(screen.getByText(/React configured/i)).toBeInTheDocument();
    expect(screen.getByText(/TypeScript enabled/i)).toBeInTheDocument();
    expect(screen.getByText(/ESLint configured/i)).toBeInTheDocument();
  });
});